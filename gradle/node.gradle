node {
    version = "12.14.0"
    npmVersion = "6.13.7"
    yarnVersion = "1.22.0"
    download = false
}

task yarnBuild(type: YarnTask, dependsOn: yarn) {
    group = "node"
    args = ["run", "build"]
}

build.doFirst { yarnBuild }
jibDockerBuild.doFirst { yarnBuild }

