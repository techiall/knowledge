jib {
    from {
        image = 'openjdk:12'
    }
    to {
        image = "techial.cc/${project.name}"
        tags = ['latest']
    }
    container {
        labels = ['maintainer': 'techial "dev@techial.cc"']
    }
}
