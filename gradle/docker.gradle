build.doLast { jibDockerBuild }

jib {
    from {
        image = 'openjdk:12'
    }
    to {
        image = "techial.top/${project.name}"
        tags = ['latest']
    }
    container {
        labels = ['maintainer': 'techial "me@techial.top"']
    }
}
